preprocess: 

function (integer) wut((integer) xpar, (integer) ypar) {
xpar = xpar + ypar;


	function (integer) myfunction((integer) z) {
	return z+5;
	};
	(integer) y;
y = myfunction(xpar);
return y;
};







module { 
(integer) xpar;
xpar = myfunction(5);

return xpar;
};